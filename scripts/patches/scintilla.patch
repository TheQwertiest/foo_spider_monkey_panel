diff --git a/submodules/scintilla/win32/scintilla.mak b/submodules/scintilla/win32/scintilla.mak
index 7e510aea..fc1b64df 100644
--- a/submodules/scintilla/win32/scintilla.mak
+++ b/submodules/scintilla/win32/scintilla.mak
@@ -40,8 +40,12 @@ CXXFLAGS=-Zi -TP -MP -W4 -EHsc -std:c++17 $(CRTFLAGS)
 CXXDEBUG=-Od -MTd -DDEBUG
 CXXNDEBUG=-O2 -MT -DNDEBUG -GL
 NAME=-Fo
-LDFLAGS=-OPT:REF -LTCG -IGNORE:4197 -DEBUG $(SUBSYSTEM) $(CETCOMPAT)
+LDFLAGS=-OPT:REF -IGNORE:4197 -DEBUG $(SUBSYSTEM) $(CETCOMPAT)
 LDDEBUG=
+LDRELEASE=-LTCG
+LIBFLAGS=
+LIBDEBUG=
+LIBRELEASE=/LTCG
 LIBS=KERNEL32.lib USER32.lib GDI32.lib IMM32.lib OLE32.lib OLEAUT32.lib
 NOLOGO=-nologo
 
@@ -58,8 +62,11 @@ CXXFLAGS=$(CXXFLAGS) -DNO_CXX11_REGEX
 !IFDEF DEBUG
 CXXFLAGS=$(CXXFLAGS) $(CXXDEBUG)
 LDFLAGS=$(LDDEBUG) $(LDFLAGS)
+LIBFLAGS=$(LIBDEBUG) $(LIBFLAGS)
 !ELSE
 CXXFLAGS=$(CXXFLAGS) $(CXXNDEBUG)
+LDFLAGS=$(LDRELEASE) $(LDFLAGS)
+LIBFLAGS=$(LIBRELEASE) $(LIBFLAGS)
 !ENDIF
 
 INCLUDES=-I../include -I../src
@@ -126,7 +133,7 @@ $(LEXCOMPONENT): $(LEXCOMPONENT_OBJS) $(DIR_O)\ScintRes.res
 	$(LD) $(LDFLAGS) -DEF:Scintilla.def -DLL -OUT:$@ $** $(LIBS)
 
 $(LIBSCI): $(COMPONENT_OBJS)
-	LIB /OUT:$@ $**
+	LIB $(LIBFLAGS) /OUT:$@ $**
 
 # Define how to build all the objects and what they depend on
 
--- a/submodules/scintilla/win32/PlatWin.cxx
+++ b/submodules/scintilla/win32/PlatWin.cxx
@@ -2530,7 +2530,7 @@ public:
        }
 };
 
-const TCHAR ListBoxX_ClassName[] = TEXT("ListBoxX");
+const TCHAR ListBoxX_ClassName[] = TEXT("SMP_ListBoxX");
 
 ListBox::ListBox() noexcept {
 }

--- a/submodules/scintilla/win32/ScintillaWin.cxx
+++ b/submodules/scintilla/win32/ScintillaWin.cxx
@@ -150,7 +150,7 @@
 
 namespace {
 
-const TCHAR callClassName[] = TEXT("CallTip");
+const TCHAR callClassName[] = TEXT("SMP_CallTip");
 
 void SetWindowID(HWND hWnd, int identifier) noexcept {
 	::SetWindowLongPtr(hWnd, GWLP_ID, identifier);
@@ -3337,7 +3337,7 @@
 	wndclass.lpfnWndProc = ScintillaWin::SWndProc;
 	wndclass.cbWndExtra = sizeof(ScintillaWin *);
 	wndclass.hInstance = hInstance;
-	wndclass.lpszClassName = L"Scintilla";
+	wndclass.lpszClassName = L"SMP_Scintilla";
 	scintillaClassAtom = ::RegisterClassExW(&wndclass);
 	const bool result = 0 != scintillaClassAtom;
 
